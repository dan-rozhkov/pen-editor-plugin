{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "pen-editor": {
      "type": "stdio",
      "command": "node",
      "args": ["${PLUGIN_ROOT}/bin/pen-editor-mcp.mjs"],
      "env": {
        "PEN_EDITOR_PLUGIN_DATA": "${PLUGIN_DATA}"
      }
    }
  }
}
